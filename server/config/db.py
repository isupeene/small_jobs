# The name of the database the server will connect to.
# This must match the corresponding value in database/config/db.py.
name = "small_jobs"

# The name of the user the server will login to the database as.
# This must match the corresponding value in database/config/db.py.
user = "small_jobs_user"

# The hostname of the computer where the database is located.
hostname = "localhost"

# The port that postgreSQL is listening on.
port = "5432"

