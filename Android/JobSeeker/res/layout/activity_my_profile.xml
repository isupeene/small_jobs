<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.smalljobs.jobseeker.views.MyProfileActivity" >


    <EditText
        android:id="@+id/profile_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:inputType="textPersonName" >
        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/profile_email_address"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/profile_name"
        android:layout_below="@+id/profile_name"
        android:layout_marginTop="14dp"
        android:hint="user@domain.com"
        android:ems="10"
        android:inputType="textEmailAddress" />

    <EditText
        android:id="@+id/profile_phone_number"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/profile_email_address"
        android:layout_below="@+id/profile_email_address"
        android:layout_marginTop="14dp"
        android:hint="Phone #"
        android:ems="10"
        android:inputType="phone" />

    <EditText
        android:id="@+id/profile_description"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/profile_phone_number"
        android:layout_below="@+id/profile_phone_number"
        android:hint="About me"
        android:ems="10"
        android:inputType="textMultiLine" />

</RelativeLayout>